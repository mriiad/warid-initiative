import { makeStyles } from '@mui/styles';
import colors from './colors';

export const mainStyles = makeStyles({
	title: {},
	subTitle: {},
	textButton: {
		cursor: 'pointer',
		color: colors.rose,
	},
});
